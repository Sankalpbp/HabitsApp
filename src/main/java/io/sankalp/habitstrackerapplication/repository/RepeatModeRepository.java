package io.sankalp.habitstrackerapplication.repository;

import io.sankalp.habitstrackerapplication.entity.RepeatMode;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepeatModeRepository extends JpaRepository<RepeatMode, Long> {
}
